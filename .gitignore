node_modules/
dist/
*.log

# editor and OS junk
.DS_Store
._*
Thumbs.db
Desktop.ini

# keep secrets out of the repo: your API key lives in an env var or an
# untracked file, never in a tracked one
.env
.env.*
.envrc
mise.local.toml
*.pem

# Claude Code's project directory is machine-local: settings in here can hold
# your course API key, and this repo goes public when you ship. Your tracked
# harness is CLAUDE.md (and any AGENTS.md), not this directory.
.claude/
.astro/

# local-only design research: scraped copies of thecrag.com used as a visual
# reference while restyling the redesign. Not part of the site, and this
# repo goes public when shipped, so keep it out of git rather than commit a
# third party's HTML/CSS/screenshots. See reference/thecrag/NOTES.md.
reference/
